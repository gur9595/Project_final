<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
	<mapper namespace="mybatis.MatchDAOImpl">
  
		<!-- 게임신청 --> 
		<insert id="gameApply" parameterType="mybatis.GameDTO"> 
			insert into game (g_idx, c_idx, g_sname, g_saddr, g_type, g_date, g_memo, g_num)
				values( 
				  	game_seq.nextval, #{c_idx}, #{g_sname}, #{g_saddr}, #{g_type}, #{g_date}, #{g_memo}, #{g_num}) 
		</insert> 
		
		<!-- 게임리스트 -->
		<select id="gameList" resultType="mybatis.GameDTO">
			select * from game
		</select>
	  
	</mapper>

